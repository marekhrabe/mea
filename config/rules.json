{
    "rules": {
        "quotes": {
            "$domain": {
                ".read": true,
                "$quote": {
                    ".write": "!data.exists()"
                }
            }
        }
    }
}
